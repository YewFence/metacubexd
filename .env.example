# Mihomo 后端 API 地址
DEFAULT_BACKEND_URL=http://127.0.0.1:9097

# Mihomo 配置文件路径（用于拉取远程配置时持久化到磁盘）
MIHOMO_CONFIG_PATH=/root/.config/mihomo/config.yaml

# 私有IP地址（可选，默认绑定到 127.0.0.1 保证安全性）
# PRIVATE_IP=100.0.0.0
